/*const loopback = require('loopback');
const mixins = require('../index');
const app = loopback();

app.use(loopback.rest());

describe ('loopback-remoting-setup-mixins', function() {

  let db;

  beforeEach(function() {
    db = app.dataSource('db', {adapter: 'memory'});
  });

  describe('enableOnly', function() {

    it('should disable all remote methods except the ones received in the options', function() {

    });

  });

  describe('disable', function() {

    it('should disable the remote methods received in the options', function() {

      mixins.disable('findOne')

    });

  });

});
*/
